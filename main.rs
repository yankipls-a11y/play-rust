
    // Line comments which go to the end of the line.
    /* Block comments which go to the closing delimiter. */



// fn stands for function
// main is a special function that gets executed immediately when you run rust programs
// use 4 spaces rather than tabs, but it doesn't matter much

fn main () {
    println!("hello world");
}